ruby '2.3.4'
source 'https://rubygems.org'

gem 'jwt'
gem 'tty' # cli interface

gem 'clipboard'
gem 'ffi', platforms: [:mswin, :mingw] # required by clipboard

group :development, :test do
  gem 'dotenv'
  gem 'pry'
end

group :test do
  gem 'fuubar' # specs progress
  gem 'rspec'
end
